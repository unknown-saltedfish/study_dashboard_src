# Dependencies (people reinstall these with npm install)
node_modules/

# Installer build output (regenerated by npm run dist)
release/
dist/

# OS/editor junk
.DS_Store
Thumbs.db
.vscode/

# Debug logs from electron-builder
builder-debug.yml
builder-effective-config.yaml

# Personal app data if it ever gets created inside the repo folder
academic-dashboard-state.json
